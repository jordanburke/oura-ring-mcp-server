{
  "mcpServers": {
    "oura": {
      "command": "node",
      "args": ["dist/index.js"],
      "env": {
        "OURA_API_KEY": "${OURA_RING_PAT}",
        "OURA_SANDBOX": "${OURA_SANDBOX:-false}"
      }
    }
  }
}
